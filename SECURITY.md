# 安全政策

筑账是个人维护的开源项目。维护者会尽力处理安全报告，但不提供商业级响应时限或可用性保证。

## 受支持的版本

当前只维护仓库默认分支的最新代码。历史阶段文档、自行部署的旧版本和已停止维护的云端验收环境不在支持范围内。

## 私下报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。报告建议包含以下信息。

- 受影响的提交、版本或部署方式
- 复现前提与最小复现步骤
- 预期结果、实际结果和影响范围
- 已尝试的缓解方法
- 方便维护者联络的方式

如果仓库尚未启用私密报告，可以创建一个题为“安全联络请求”的公开 Issue，只留联系方式。不要在 Issue 中公开利用细节、个人数据、云端标识、密钥或可执行的恶意样本。

## 处理方式

维护者会先确认报告是否可复现，随后评估影响、准备修复和回归测试。修复就绪前，请不要公开漏洞细节。维护者可能要求补充信息，也可能将不在项目代码范围内的问题转给相应依赖或云服务商。

## 重点关注范围

- 身份认证、会话、CSRF 与权限边界
- 文件上传、解析、下载和对象存储隔离
- 项目、家庭成员和删除撤销期间的数据隔离
- 密钥、SMTP 凭据、模型 API 凭据和云端运行凭据泄漏
- 可以跨越配额、类型检查或恶意文件扫描的上传路径

请不要在未授权的系统上执行压力测试、社会工程、账号接管或不可逆的数据操作。

## 部署者需要知道

面试演示环境建议设置 `UPLOADS_ENABLED=false`，后端会在读取或存储新文件前拒绝上传。如果在生产环境设置 `UPLOADS_ENABLED=true`，必须配置 ClamAV 并完成正常样本、EICAR 测试样本和扫描服务不可用三条路径的验收。

不要把 `.env`、真实上传文件、数据库、运行日志或云端导出制品提交到 Git。
