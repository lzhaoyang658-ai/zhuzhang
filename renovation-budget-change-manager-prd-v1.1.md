# 筑账｜装修预算与增项管家 PRD V1.1

> 文档类型｜当前实现校准版产品需求文档<br>
> 基线版本｜《装修预算与增项管家 PRD V1.0》<br>
> 更新日期｜2026-09-02<br>
> 产品状态｜开源作品集模式进入发布验收，完整生产上传仍待部署<br>
> 发布建议｜本地开发与关闭上传的作品集演示可发布；开放文件上传和公开 Beta 继续 HOLD<br>
> 部署状态｜代码支持零新增扫描费用的作品集模式；完整上传模式仍需 ClamAV 与告警<br>
> 首发平台｜响应式 Web，覆盖桌面与手机浏览器<br>
> 目标用户｜首次进行全屋装修、负责预算与付款的业主<br>

---

## 0. 文档说明

### 0.1 本版用途

这份文档把旧版 PRD、阶段 1 至阶段 16 的开发结果、当前代码和 2026-08-31 线上 UAT 合并为同一份产品基线。它同时回答三件事。

- 用户现在能完成什么。
- 哪些能力已有代码，但当前线上形态尚未启用。
- 哪些旧版需求仍待补齐，进入公开 Beta 或正式版前需要什么证据。

旧版 PRD 保留，不在本次更新中覆盖。研发、测试和产品验收应以本文件的状态标记为准。

### 0.2 状态定义

| 状态 | 判定标准 |
|---|---|
| 已实现并验证 | 当前代码可用，并有自动化、浏览器或线上 UAT 证据 |
| 核心已实现 | 主路径可用，旧版 PRD 中仍有高级规则或边界未完成 |
| 代码已准备 | 代码和配置入口存在，当前线上部署没有启用 |
| 待实现 | 当前代码中没有完整用户路径 |
| 延后 | 不进入 V1.1，达到明确条件后再评估 |

### 0.3 证据边界

已经确认的事实包括安全邮箱登录、项目创建、三报价、两版基线、内部增减项确认、预算联动、验收付款、私有证据、风险通知、正式档案和备份恢复均完成过线上或自动化验收。外部确认的后端和公开页已经验收，并已补充版本并发控制与重复提交保护；项目工作台的发起入口仍未完成。

仓库中没有真实用户访谈、持续使用、真实付费或留存数据。因此，工程可行性已经验证，真实需求强度、长期使用意愿和项目制付费意愿仍是待验证项。旧版 V1.0 中“全部核心需求均已验证”的表述不再作为事实结论。

### 0.4 2026-09-02 实现校准

本次校准把仓库整改结果纳入产品基线，主要变化如下。

- 隔离演示数据与正式身份，修正减项符号和金额精度。
- 为报价、合同基线、付款与冲正补齐服务端一致性、幂等和并发约束。
- 补齐项目删除的原件、档案、本地缓存和对象历史版本清理，并加入周期维护器。
- 为上传增加文件特征、MIME、压缩包与图片边界校验，保存 SHA-256 和扫描状态，并设置项目级硬配额。
- 增加近期登录、邀请身份防接管、可信代理范围、稳定生产密钥校验和安全响应头。
- 增加数据库版本门禁、与启动目录无关的路径解析、前后端自动化测试和持续集成工作流。
- 未建立合同基线时明确显示“尚未确认”，不再把空值渲染成 `Vnull`。
- 增加 `UPLOADS_ENABLED` 部署开关。生产环境关闭上传时，页面不提供上传入口，后端在读取文件前拒绝请求；其他项目管理能力继续运行。

以上结论只指当前仓库代码及本地验收，不能把“代码已实现”写成“某个线上环境已启用”。公开版本不记录历史环境的密钥状态、资源标识或网络拓扑。

### 0.5 历史部署评估（公开脱敏）

开源整理前曾对一个私有验收环境做只读核对，没有修改环境变量、发布版本或云资源。为避免把历史环境误认为当前公共服务，本节只保留产品结论。

- 历史验收只能证明当时版本可用，不能证明当前代码已经发布，也不构成公共演示地址承诺。
- 作品集部署必须显式设置 `UPLOADS_ENABLED=false`；完整生产上传必须显式设置 `UPLOADS_ENABLED=true` 并提供可用的 ClamAV。
- 密钥、SMTP、对象存储、网络与健康探针配置必须由部署者在自己的环境中重新核对，不能从公开仓库推断。
- 本地 `.env.production.local` 被 Git 忽略，只能保存本机非公开配置，不能作为远端完整环境的备份。

ClamAV 的生产方案以 veFaaS 官方[函数网络与健康配置](https://www.volcengine.com/docs/6662/1206174)、[微服务容器部署](https://www.volcengine.com/docs/6662/1338568)、[私网 CLB TCP 访问](https://www.volcengine.com/docs/6662/1923772)和 [ClamD 协议](https://docs.clamav.net/manual/Usage/ClamdProtocol.html)为依据。当前不采用官方文档没有说明的 Sidecar 方案。

### 0.6 开源作品集发布决策

本项目当前用于个人面试展示，并计划公开源代码。代码公开和在线演示不再以购买常驻 ClamAV 资源为前置条件。发布形态分为三档。

| 形态 | 上传策略 | 扫描策略 | 发布边界 |
|---|---|---|---|
| 本地开发 | 可上传本地可信样本 | 可显式关闭 | 不作为公网生产环境 |
| 作品集演示 | `UPLOADS_ENABLED=false` | 不适用 | 保留项目、预算、增减项、验收和档案浏览，关闭所有文件上传 |
| 完整生产 | `UPLOADS_ENABLED=true` | 强制 ClamAV | 扫描不可用时拒绝上传，就绪检查进入降级或不可用状态 |

开源使用者自行选择部署形态并承担自己的基础设施费用。仓库提供 ClamAV 适配镜像和部署说明，不替使用者托管扫描服务。公开仓库不得包含真实云端地址、资源标识、用户数据或凭据。

---

## 1. 产品定义

### 1.1 产品命题

筑账是一款由装修业主持有的预算与增项管理工具。它把候选报价、合同基线、增减项确认、阶段验收、付款事实、原始证据和项目档案连成一条可追溯记录链，帮助业主在施工和付款前看清依据、条件与资金影响。

### 1.2 最适合的用户

- 正在进行首次全屋装修。
- 由自己或家庭成员负责核对报价、批准增项和安排付款。
- 目前主要依赖 Excel、微信聊天、相册和纸质合同。
- 担心报价遗漏、口头增项、付款条件不清和最终超支。
- 愿意保留关键记录，但没有专业造价或项目管理工具。

### 1.3 用户核心任务

当装修公司、工长或供应方提出新费用、申请进入下一阶段或要求付款时，业主需要快速确认费用是否属于现有合同、是否完成确认、施工条件是否达到，以及记录之后预算会发生什么变化。

### 1.4 替代方式与差异

| 当前替代方式 | 常见问题 | 筑账提供的结果 |
|---|---|---|
| Excel 或记账表 | 金额可计算，确认过程和原始依据容易分散 | 金额、状态、版本和证据统一关联 |
| 微信聊天和相册 | 信息按会话堆积，后续难以定位 | 外部确认链接、时间线和原件目录 |
| 装修公司内部系统 | 数据控制权和口径由服务方掌握 | 业主持有独立底账 |
| 纸质合同与付款截图 | 能留存单件材料，难以形成完整过程 | 基线、增减项、验收、付款和档案串联 |
| 专业造价或监理服务 | 专业度高，成本和可获得性有限 | 提供日常记录与风险提示，不替代专业判断 |

### 1.5 产品原则

1. 付款前先展示条件与风险。
2. 原文件是依据，结构化结果必须能够回到来源。
3. AI 只生成待校对草稿，正式金额由人确认。
4. 重要记录通过新版本、冲正或追加事件更正。
5. 平台记录事实，不替代律师、监理、造价师或电子认证服务。
6. 项目数据可导出，删除具有撤销期和最小化留痕。

### 1.6 非目标

- 装修公司 ERP、进销存、工资或供应商管理。
- 建材交易撮合、装修款支付、代收、托管或担保。
- 自动判定施工质量、合同效力或价格合理性。
- 自动决定是否批准增项或是否付款。
- 地区市场价数据库和开放式装修顾问。
- 原生 App、微信小程序和任意多级审批流。

---

## 2. 版本目标与范围

### 2.1 V1.1 用户结果

V1.1 保留五项必须完整工作的能力。

| 编号 | 必备能力 | 用户结果 | 当前状态 |
|---|---|---|---|
| M1 | 报价导入、对比与合同基线 | 从最多三份报价建立可回溯的预算起点 | 核心已实现 |
| M2 | 预算总账与风险预警 | 看清批准预算、待确认金额、预测结算和资金余量 | 已实现并验证 |
| M3 | 增减项确认 | 在实施或付款前记录范围、金额、工期和确认结果 | 核心已实现 |
| M4 | 验收与付款节点 | 付款前检查验收与问题，付款后保留事实和冲正 | 核心已实现 |
| M5 | 证据、时间线与项目档案 | 随时回查原件并生成可携带的完整项目包 | 已实现并验证 |

账号、项目生命周期、家庭协作、通知和安全会话属于支撑能力。项目购买权益尚未实现，不得在页面或对外材料中描述为已上线。

### 2.2 当前发布结论

| 发布层级 | 结论 | 说明 |
|---|---|---|
| 本地开发与内部验收 | GO | 核心路径和回归测试可运行 |
| 开源代码与面试评审 | GO | 完成许可证、脱敏、干净克隆和首次提交检查后发布 |
| 关闭上传的在线作品集 | GO | 使用生产认证和安全配置，显式关闭上传与 AI 文件解析，不产生 ClamAV 新增费用 |
| 个人及小范围真实使用 | HOLD | 完整上传模式尚缺目标环境中的 ClamAV、告警和重部署验收 |
| 公开 Beta | HOLD | 先完成小范围重部署，再补真实报价评测、周期运维和关键数据完整性 |
| 公开付费正式版 | INVESTIGATE | 需补付费验证、权益系统、条款、指标基线和规模化运行证据 |

### 2.3 V1.1 不纳入范围

- 短信登录和密码体系。当前采用邮箱无密码登录，因此没有密码重置。
- 项目权益购买、退款、发票和权益转移。
- 认证电子签名、可信时间戳和第三方存证。
- 银行账户、支付渠道或装修款托管。
- 真实地区价格判断和自动施工质量识别。
- 多实例自动扩容。相关 PostgreSQL 与独立 Worker 代码保留为扩容准备。

---

## 3. 用户与权限

### 3.1 角色

| 能力 | 项目所有者 | 共同管理者 | 只读成员 | 外部确认人 |
|---|---|---|---|---|
| 查看项目完整业务数据 | 是 | 是 | 是 | 否 |
| 编辑报价、预算、增减项、验收、付款和证据 | 是 | 是 | 否 | 否 |
| 内部确认增减项 | 是 | 是 | 否 | 否 |
| 管理家庭成员 | 是 | 否 | 否 | 否 |
| 修改项目资料和资金上限 | 是 | 否 | 否 | 否 |
| 归档、恢复和申请删除 | 是 | 否 | 否 | 否 |
| 生成完整项目档案 | 是 | 否 | 否 | 否 |
| 查看指定增减项 | 是 | 是 | 是 | 是 |
| 对指定增减项提交决定 | 是 | 是 | 否 | 是 |

### 3.2 权限规则

- 每个项目最多有 5 位家庭协作者，不含项目所有者。
- 家庭邀请默认 7 天有效，数据库仅保存令牌摘要。
- 非项目成员访问项目或下属对象时返回统一的无权访问结果，不暴露对象是否存在。
- 只读成员的所有写请求由服务端拒绝。
- 已归档项目保持只读，仍可查看和生成档案。待删除项目可查看并下载有效期内的已有档案，但不能生成新档案。
- 成员管理、项目资料、归档删除和档案导出要求近期登录。
- 外部确认页不加载项目会话，只返回指定增减项所需的最少信息。

---

## 4. 关键业务概念与计算口径

### 4.1 业务概念

| 概念 | 定义 |
|---|---|
| 装修资金上限 | 用户为项目设置的最高资金边界 |
| 风险预留金 | 用户单独记录的计划缓冲金额，当前不重复从资金余量中扣减 |
| 候选报价 | 签约前用于校对和比较的报价原件及结构化草稿 |
| 合同基线 | 当前生效的合同预算版本，可来自已确认报价或人工金额 |
| 已批准预算 | 合同基线加上已批准增项净变化 |
| 待审批风险 | 待确认和修订中增减项的净金额 |
| 预测结算 | 已批准预算加上待审批风险 |
| 付款节点 | 计划日期、金额、条件和验收要求的组合 |
| 受控付款 | 付款前检查结果为 ready 或 warning、没有进入 high_risk 的有效付款 |
| 证据 | 用户上传并可关联到业务对象的原始文件 |
| 审计事件 | 对关键动作追加记录的项目事实 |

### 4.2 金额公式

```text
合同基线 = 当前有效基线金额

已批准预算 = max(0, 合同基线 + 已批准增加项 - 已批准减少项)

待审批风险 = 待确认增加项 - 待确认减少项

预测结算 = max(0, 已批准预算 + 待审批风险)

剩余资金 = 装修资金上限 - 预测结算

已批准超支率 = 已批准增项净变化 / 合同基线

预测超支率 = (预测结算 - 合同基线) / 合同基线

付款进度 = 累计有效实付 / 已批准预算
```

边界规则如下。

- 合同基线为 0 时不计算超支率。
- 金额以人民币分保存，前端展示到元。
- 付款冲正从累计实付中扣除。
- 已拒绝、已撤回的增减项不进入预算。
- 预算由服务端统一计算，页面和报告复用同一口径。
- 风险预留金当前用于计划提示，不参与上述公式。

### 4.3 核心状态

```text
项目
准备中 / 施工中 / 待结算 → 已归档 → 施工中
准备中 / 施工中 / 待结算 / 已归档 → 待删除 → 原状态

报价任务
queued → running → succeeded
queued → running → queued 延迟重试
queued → running → dead_letter → 人工重新排队

增减项
draft → pending_confirmation
pending_confirmation → approved / rejected / revising / withdrawn
revising → pending_confirmation / withdrawn
approved → implemented → accepted → settled

档案任务
queued → running → succeeded
queued → running → queued 延迟重试
queued → running → dead_letter
succeeded → expired → 人工重新生成
```

---

## 5. 信息架构与核心流程

### 5.1 当前页面

```text
账号
├── 邮箱登录
├── 设备会话
├── 项目中心
├── 通知中心
└── 档案工作台

项目工作台
├── 项目总览
├── 报价与预算
├── 增减项
├── 验收与付款
├── 证据与时间线
├── 成员与权限
└── 项目设置

公开最小权限页面
├── 家庭邀请接受页
└── 增减项外部确认页
```

### 5.2 项目启动

```text
邮箱验证码登录
  ↓
没有项目时进入三步建项
  ↓
填写房屋信息、计划时间和资金边界
  ↓
生成 12 个预算分类、5 个付款节点和所有者关系
  ↓
上传最多 3 份候选报价
  ↓
解析草稿 → 人工校对 → 报价确认
  ↓
对比 2 至 3 份报价 → 选择报价建立合同基线
  ↓
进入预算、增减项、验收付款和证据管理
```

### 5.3 增减项确认

内部确认可以从项目工作台完成。外部确认目前需要通过后端接口生成链接，公开页可完成决定，普通用户尚不能从工作台发起。

```text
创建增减项草稿
  ↓
内部处理，或通过后端接口生成外部确认链接
  ↓
同意 → 进入已批准预算
拒绝 → 保留记录，不进入预算
要求补充 → 进入修订中，重新发送
  ↓
实施 → 验收 → 结算
```

### 5.4 付款与归档

```text
付款节点临近
  ↓
检查验收、未关闭问题、待确认增减项、节点余额和累计预算
  ↓
正常记录 / 填写高风险继续原因
  ↓
更新累计付款、风险通知和时间线
  ↓
生成正式档案 → 项目归档
```

---

## 6. 功能需求

### 6.1 S1 账号与项目生命周期

#### FR-S1.1 邮箱无密码登录

**优先级** P0<br>
**状态** 已实现并验证

需求规则如下。

- 用户通过邮箱申请 6 位数字验证码。
- 验证码 10 分钟有效，单次最多尝试 5 次。
- 同一邮箱 10 分钟最多申请 5 次，同一来源 10 分钟最多申请 20 次。
- 验证码使用原子消费；并发提交只能有一个请求成功，不能重复签发会话。
- 验证码、会话令牌和 CSRF 令牌均不以明文保存。
- 登录会话默认有效 30 天，Cookie 使用 HttpOnly、SameSite=Lax，线上只允许 HTTPS。
- 用户可查看有效设备，撤销其他设备，或退出当前设备。
- 修改资金上限、成员权限、项目删除和正式档案等敏感操作要求当前会话完成近期邮箱验证，挑战不能抬高其他设备的会话权限。
- 正式环境必须关闭演示身份 Header。

验收标准如下。

- 未登录访问项目、通知和档案页面时进入登录页。
- 验证码错误、过期或超出次数时给出明确结果。
- 写请求缺少有效 CSRF 令牌时被服务端拒绝。
- 撤销会话后，该设备无法继续读取项目。

#### FR-S1.2 创建和切换项目

**优先级** P0<br>
**状态** 已实现并验证

必填字段包括项目名称、城市、面积、面积口径、装修方式和资金上限。计划开工、计划完工、地址、风险预留金和备注可按界面要求填写。

创建规则如下。

- 面积大于 0 且不超过 10000 平方米。
- 计划完工日期不得早于计划开工日期。
- 风险预留金不得高于资金上限。
- 创建后自动生成所有者关系、资金上限初始记录、12 个预算分类和 5 个付款节点。
- 默认预算分类为拆除与新建、水电、泥瓦、木作、油漆、门窗、厨卫、主材、软装、家具家电、设计与管理、其他。
- 默认付款节点为开工款、水电阶段款、泥木阶段款、竣工款和尾款。
- 一个账号可以访问多个项目，项目中心显示角色、状态、资金上限、预测结算和累计付款。

#### FR-S1.3 项目设置和资金历史

**优先级** P0<br>
**状态** 已实现并验证

- 只有项目所有者可以修改项目资料和资金上限。
- 修改资金上限必须填写原因，并记录修改前金额、修改后金额、操作者和时间。
- 共同管理者可以调整分类预算，所有者也可以执行。
- 分类预算可以为空，表示暂不设置上限。
- 项目状态不会根据日期、验收或付款自动流转。
- 当前项目页面没有在准备中、施工中和待结算之间切换的入口。公开 Beta 前应补齐手动切换，或把未使用状态从用户界面移除。

#### FR-S1.4 归档和删除

**优先级** P0<br>
**状态** 核心已实现

- 项目归档后只读，仍可查看和导出。
- 只有所有者可以恢复归档项目，恢复后的状态为施工中。
- 删除申请要求准确输入项目名称。
- 删除申请进入 7 天撤销期，期间项目只读。
- 撤销期内允许查看项目和下载仍有效的既有档案，不允许生成新的档案任务。
- 到期后应删除业务数据、原始附件和导出制品，只保留不含名称、地址和正文的最小化删除记录。
- 当前清理器会删除活动数据库记录、原始附件、导出制品、本地缓存和暂存文件；S3 兼容对象存储会清理当前对象、历史版本和删除标记。
- 服务启动时先执行一次清理，运行期间默认每 10 分钟再次执行。删除中断会保留项目并允许后续重试，不会先删数据库再遗留对象。
- 公开 Beta 前仍需在部署平台接入周期任务健康监控与失败告警，并在隐私政策中明确数据库恢复点的最终清理期限。

---

### 6.2 M1 报价导入、对比与合同基线

#### FR-1.1 报价上传与任务

**优先级** P0<br>
**状态** 核心已实现

- 支持 CSV、XLSX、文本 PDF、扫描 PDF、JPG、JPEG、PNG 和 HEIC。
- 每次上传 1 个文件，单文件不超过 30 MB。
- 一个项目最多保留 3 份非失败候选报价。
- 上传内容必须同时通过扩展名、文件特征和 MIME 校验；图片限制解码像素，XLSX 与 DOCX 限制压缩条目、单条解压大小和总解压大小。
- 报价源文件和证据文件共用项目级硬配额，默认最多 200 个文件、合计 1 GB；配额预占、对象写入和数据库提交失败时需要补偿回滚。
- 每个原件保存 SHA-256、实际类型、扫描状态和扫描时间。生产环境只要开启上传，就必须启用 ClamAV；扫描不可用或结果不干净时拒绝上传。
- 作品集部署可以关闭上传。页面需要解释当前环境未开放文件上传，后端必须在解析 multipart 请求体前返回 `503 / FILE_UPLOADS_DISABLED`，不能依赖前端隐藏按钮。
- 上传后立即返回持久任务，页面轮询排队、解析、保存、成功或失败进度。
- 用户离开页面不会取消任务。
- 任务默认最多尝试 3 次，支持指数退避、租约回收、死信和人工重新排队。
- 解析失败的报价保留失败原因，不占有效报价名额。

当前未实现批量上传最多 20 张图片。HEIC 在运行环境无法解码时应提示先转为 JPG 或 PNG，不得生成猜测结果。

#### FR-1.2 报价解析与人工校对

**优先级** P0<br>
**状态** 核心已实现

系统提取以下字段。

- 原始项目名称和标准项目名称。
- 空间、预算类别、数量、单位、单价和合价。
- 材料信息和工艺说明。
- 原始页码、表格行或图片条目位置。
- 原文摘录、字段置信度、解析方式和解析器版本。

处理规则如下。

- CSV、XLSX 和可解析文本 PDF 优先使用确定性规则。
- 扫描件和图片在启用模型时使用视觉模型，低质量结果升级复核，服务异常时回退到本地 OCR。
- 扫描 PDF 本地 OCR 最多处理前 20 页，超出时给出警告。
- 数量和单价可用时，后端重新计算合价。
- 数量乘单价与合价不一致时降低置信度并要求人工复核。
- 用户修改字段后记录前值、后值、操作者和时间。
- 关键字段完整后才能确认报价。
- 已确认报价不能直接修改。

本版尚未实现批量分类、条目合并、条目拆分和疑似重复项删除。

#### FR-1.3 报价对比

**优先级** P0<br>
**状态** 核心已实现

- 用户选择 2 至 3 份不同报价进行对比。
- 系统展示总价差异、匹配组、未覆盖报价和组内价差。
- 系统对未人工分组条目生成相似匹配建议。
- 用户可确认 2 至 3 个跨报价条目的匹配关系，也可解除人工关系。
- 同一人工匹配组不能含同一报价中的两个条目。
- 已人工匹配的条目不再被系统建议覆盖。
- 对比结果不得输出签约建议、绝对合理价格或施工质量判断。

本版尚未实现报价差异 PDF 或 CSV、追问问题清单、批量筛选和独立重复项处理。

#### FR-1.4 合同基线

**优先级** P0<br>
**状态** 核心已实现

- 已确认报价可直接生成新基线版本，基线金额等于报价有效条目合计。
- 用户也可输入金额和原因建立人工基线。
- 同一时刻只有一个有效基线。
- 每次激活都生成递增版本，旧版本保留并进入时间线和正式报告。
- 从报价建立的基线保留来源报价 ID，用于分类预算计算。
- 未确认报价不能设为基线。

公开 Beta 前需要补齐以下数据完整性要求。

- 正式合同或补充协议的关联入口。
- 含税口径、报价与合同差额及原因。
- 基线条目快照及旧版条目映射。
- 基线升级前对已有增减项的影响预览。
- 无法映射的历史增减项待重新关联状态。

---

### 6.3 M2 预算总账与风险预警

#### FR-2.1 预算总览

**优先级** P0<br>
**状态** 已实现并验证

项目总览展示以下金额和比例。

- 装修资金上限。
- 当前合同基线及版本。
- 已批准增减项净变化。
- 已批准预算。
- 待审批风险。
- 预测结算。
- 累计已付款。
- 剩余资金。
- 未来 30 天计划付款。
- 预测结算相对资金上限的位置、预测余量或超支额，以及付款进度。

页面必须明确区分待审批风险、已批准预算和已付款，不得把预测金额表达为实际支出。

#### FR-2.2 分类预算

**优先级** P0<br>
**状态** 已实现并验证

- 每个项目初始化 12 个固定一级分类。
- 用户可以为分类设置或清空计划上限。
- 来源报价的分类金额，加上已批准和待确认分类增减项，构成已知分类预测金额。
- 人工基线或历史基线没有来源报价时，不猜测基线分类分布。
- 项目设置同时展示分类已知预测金额和用户设置的分类上限；增减项先按分类计算净额，再对最终结果执行零下限，计算不依赖记录顺序。
- 分类预测超过上限时生成 A5 风险。

#### FR-2.3 A1 至 A8 风险

**优先级** P0<br>
**状态** 已实现并验证

| 编号 | 触发条件 | 级别 | 定位对象 |
|---|---|---|---|
| A1 | 增减项处于待确认或修订中 | 提示 | 增减项 |
| A2 | 已批准预算高于合同基线 | 关注 | 项目预算 |
| A3 | 预测结算达到资金上限 90%，且未超限 | 警告 | 项目预算 |
| A4 | 预测结算超过资金上限 | 严重 | 项目预算 |
| A5 | 已知分类预测金额超过分类上限 | 警告 | 预算分类 |
| A6 | 未付清节点已到期或 7 天内到期，验收缺失、不通过或仍有问题 | 警告 | 付款节点 |
| A7 | 待确认增减项超过 48 小时 | 提示 | 增减项 |
| A8 | 节点有效实付超过节点计划金额 | 严重 | 付款节点 |

风险规则如下。

- A3 和 A4 互斥。
- 每位项目成员收到独立通知。
- 同一用户、项目、规则和对象使用唯一去重键。
- 风险消失后转为已解除并保留记录。
- 同一风险再次出现时复开原通知并增加出现次数。
- 项目归档或进入待删除状态后不再生成活动风险。

当前风险协调由项目总览和通知请求触发。公开 Beta 前应增加业务事件触发和周期兜底。

---

### 6.4 M3 增减项确认

#### FR-3.1 创建增减项

**优先级** P0<br>
**状态** 已实现并验证

必填字段包括类型、标题、原因、内容、金额、类别、提出人、提出日期和工期影响天数。区域可为空。没有附件时必须保留明确确认标记。

规则如下。

- 类型只允许增加或减少。
- 金额必须大于 0，预算服务根据类型决定正负方向。
- 工期影响范围为减少 365 天至增加 365 天。
- 草稿创建后写入时间线。
- 已批准、已实施、已验收和已结算状态均进入已批准预算。
- 待确认和修订中状态进入待审批风险。
- 当前界面没有直接上传增减项附件，因此用户必须勾选“当前没有附件”；接口缺少该字段或传入 false 时拒绝创建。

#### FR-3.2 状态转换

**优先级** P0<br>
**状态** 已实现并验证

- 草稿或修订中可以发送确认。
- 待确认可以批准、拒绝、要求补充或撤回。
- 已批准可以标记实施。
- 已实施可以标记验收。
- 已验收可以标记结算。
- 非法状态转换由服务端拒绝。
- 每次状态变化写入审计事件。

#### FR-3.3 外部确认链接

**优先级** P0<br>
**状态** 核心已实现

- 后端可为草稿、修订中或待确认增减项生成不可预测链接，公开确认页已经可用。
- 链接默认 7 天有效，数据库只保存令牌摘要。
- 外部页面只展示固定泛化名称“装修项目”和指定增减项，不返回用户填写的项目名、城市或地址。
- 外部确认人无需注册，提交姓名、角色、意见和当前版本确认。
- 决定支持同意、拒绝和要求补充。
- 决定请求必须携带当前版本；服务端用条件更新处理并发，过期版本返回冲突。
- 处理完成后同一链接只读，重复或并发提交不会重复入账。
- 审计事件记录确认人、角色、时间和 IP 摘要。
- 页面明确说明普通网页确认不等于认证电子签名。
- 前后端响应设置禁止索引和禁止被其他页面嵌入的安全策略。

公开 Beta 前需要补齐以下能力。

- 在项目工作台增加“生成确认链接”、复制、重新生成和状态反馈入口。当前普通用户无法从项目界面发起外部确认。
- 增减项关键字段和附件的不可变版本快照。
- 修改金额、范围、工期或附件后的自动升版和旧链接失效说明。
- 链接主动撤销和延长入口。
- 外部确认页的关联附件受控预览。
- 记录链接查看事件和 User-Agent 摘要。当前只在提交决定时记录 IP 摘要。
- 家庭共同管理者加外部确认人的两级串行策略。

当前增减项记录的版本号保持为 1，状态转换不会创建新版本。页面不得在该能力补齐前宣称每次修订均已保存完整版本。

---

### 6.5 M4 验收与付款节点

#### FR-4.1 付款节点

**优先级** P0<br>
**状态** 核心已实现

- 创建项目时按资金上限生成 20%、30%、25%、20% 和 5% 的五个默认节点。
- 节点包含名称、计划金额、计划日期、付款条件和验收要求。
- 用户可新增付款节点。
- 单个节点支持多次付款。
- 当前版本没有节点编辑、删除、排序和比例随批准预算联动功能。
- 创建项目和付款节点页面不得在编辑能力上线前声称节点金额或日期可以继续修改。
- 有付款记录的节点不得通过数据层静默删除。

#### FR-4.2 验收记录

**优先级** P0<br>
**状态** 核心已实现

- 验收结果支持通过、带问题通过和不通过。
- 带问题通过时未关闭问题数至少为 1。
- 验收记录包含日期、说明、未关闭问题数、记录人和系统时间。
- 付款前检查使用最新一条验收记录。
- 新记录追加保存，不覆盖旧记录。

本版以问题数量代替问题明细。后端允许追加验收记录，项目页面在已有验收后缺少再次记录入口。公开 Beta 前应补齐复验入口，并增加问题名称、状态、处理说明、忽略原因、附件和版本。

#### FR-4.3 付款前检查

**优先级** P0<br>
**状态** 已实现并验证

检查项包括有效验收记录、未关闭问题、项目内待确认增减项、节点计划余额和累计付款是否超过已批准预算。

结果分为以下三档。

- ready 表示全部检查通过。
- warning 表示存在待确认增减项或其他提醒，仍可记录。
- high_risk 表示验收缺失、不通过或仍有问题，必须填写继续记录原因。

当前检查使用项目内全部待确认增减项，没有判断它是否与节点相关。付款凭证也没有成为服务端强制检查项。这两项进入公开 Beta 的数据完整性补齐范围。

#### FR-4.4 付款事实和冲正

**优先级** P0<br>
**状态** 核心已实现

- 付款记录包含金额、日期、收款方、方式、流水号或备注。
- 文案统一使用“已记录付款”。
- 平台需要允许用户记录已经发生的真实付款。
- 本次付款超过节点剩余计划金额时，应进入高风险确认并要求填写原因，同时生成 A8 风险。
- 高风险付款缺少继续原因时由服务端拒绝。
- 写入可携带幂等键，重复请求不能产生重复付款。
- 冲正创建反向记录，不覆盖原付款。
- 同一原付款只允许存在一条有效冲正。重复提交应返回已有结果或明确拒绝。
- 冲正后累计付款实时回退。

付款和冲正均已要求幂等键，并以请求指纹、项目范围唯一约束和原付款唯一冲正关系抵御重复写入。当前代码仍会在付款超过节点剩余金额时直接拒绝写入，与上述事实记录原则不一致。公开 Beta 前需要改为高风险事实记录，或经过产品决策后明确保留硬拦截并提供调整节点的完整路径。

---

### 6.6 M5 证据、时间线与项目档案

#### FR-5.1 证据文件

**优先级** P0<br>
**状态** 核心已实现

- 支持 JPG、JPEG、PNG、HEIC、PDF、XLSX、DOCX 和 CSV。
- 单文件不超过 30 MB。
- 文件保存原始文件名、随机对象键、类型、大小、证据类型、说明和一个可选关联对象。
- 文件保存 SHA-256、真实 MIME、扫描状态和扫描时间；下载前再次检查生产扫描状态。
- 生产环境的原始文件存放在私有对象存储，本地开发存放在被 Git 忽略的数据目录；下载前校验项目权限，并使用短时签名地址或受控文件流。
- 修改业务说明不会覆盖原件。
- 报价源文件和证据共用项目级文件数量与容量配额，避免绕过任一入口无限占用存储。

当前项目上传界面只提供付款节点关联，通用接口可以保存一个关联类型和对象。尚未实现多对象关联、受控预览图和单文件删除前引用检查。开发环境允许显式跳过恶意文件扫描；生产环境只有开启上传时才必须连接 ClamAV，并在恢复真实文件使用前完成目标环境验收。

#### FR-5.2 项目时间线

**优先级** P0<br>
**状态** 已实现并验证

- 展示报价任务、人工校对、匹配关系、基线、增减项、验收、付款、证据、成员、项目状态、导出和删除请求等关键事件。
- 事件按系统记录时间倒序展示。
- 事件包含对象、操作者、说明和可选金额变化。
- 关键事件不能从普通产品流程单独删除。

当前页面没有按日期、事件类型、区域、参与人和业务对象筛选。

#### FR-5.3 正式项目档案

**优先级** P0<br>
**状态** 已实现并验证

- 只有项目所有者可生成档案，创建和重试要求近期登录。
- 用户可选择全部时间或日期范围，并选择是否包含附件。
- 任务异步执行，支持离开页面、进度轮询、失败重试、租约回收和死信。
- 成功制品默认 24 小时内可下载，过期后保留任务历史并允许重新生成。
- 私有对象存储每次生成短时下载地址，当前默认有效 5 分钟。档案任务的 24 小时可下载期与单次地址有效期应分别表达。
- 主卷包含正式多页 PDF、预算与增减项 CSV、验收与付款 CSV、时间线 CSV、附件目录和导出清单。
- 用户选择包含附件时，原始报价和证据文件进入档案。
- 正式报告包含项目概况、预算边界、全部基线版本、增减项、节点、验收、付款与冲正、附件目录、时间线和使用边界。
- 图片证据生成等比例缩略图，其他附件保留目录和原件。
- 每个制品记录大小、SHA-256、存储后端、报告版本和页数。
- 下载前重新校验完整性，校验失败时拒绝下载。
- 超过配置阈值的项目拆成可独立打开的主卷和附件卷，正式报告始终位于主卷。
- 文件名不得包含完整项目地址。

当前报告版本为 formal-v2，默认分卷阈值为 100 MB。SHA-256 用于校验文件一致性，不代表数字签名或可信存证。

---

### 6.7 S2 家庭协作与通知

#### FR-S2.1 家庭邀请

**优先级** P1<br>
**状态** 已实现并验证

- 所有者通过邮箱创建共同管理者或只读成员邀请。
- 邀请 7 天有效，可在接受前撤销。
- 接受邀请前必须先用受邀邮箱登录；服务端校验当前会话邮箱与邀请邮箱完全一致。
- 接受邀请只建立或关联项目成员关系，不签发新会话，不允许公开邀请页修改账号姓名或接管既有账号。
- 所有者可调整协作者角色或移除成员。
- 邀请、加入、改权、移除和撤销写入项目时间线。

#### FR-S2.2 通知中心

**优先级** P1<br>
**状态** 已实现并验证

- 支持跨项目查看、项目筛选、处理中、已解除和全部状态。
- 支持单条已读和全部已读。
- 风险通知与事件通知分开处理。
- 报价解析、档案生成和成员权限变化会创建事件通知。
- 通知行动链接返回对应项目页签或档案任务。
- 未读数显示在工作台入口。

#### FR-S2.3 邮件摘要

**优先级** P1<br>
**状态** 核心已实现

- 邮件摘要默认关闭。
- 用户可以开启每天或每周摘要。
- 摘要只包含风险级别、项目名称、标题和说明，不带合同、照片、地址或敏感附件。
- 投递复用登录邮箱的 SMTP 配置。
- 投递成功后才更新时间，失败后保留重试机会。

应用进程不会自行启动定时器。当前部署需要由平台周期任务调用摘要执行器，公开 Beta 前必须确认调度、失败重试和告警已启用。

---

### 6.8 S3 项目权益

#### FR-S3.1 项目购买权益

**优先级** P2<br>
**状态** 待实现

当前版本没有试用、购买、退款、发票、商业权益配额和权益转移。系统已有防滥用的项目级文件硬上限，但它不代表付费套餐。公开付费前需要重新验证商业模式，并单独定义以下内容。

- 一套房对应一个项目权益的价格和有效期。
- 试用可用范围和数据保留规则。
- 三报价、外部确认、证据容量和完整导出的权益边界。
- 退款、发票、权益转移、支付失败和重复支付处理。
- 软件购买支付与装修付款记录的彻底隔离。

未完成前，所有现有功能按 Beta 访问策略开放，不在客户端伪造权益状态。

---

## 7. AI 报价解析要求

### 7.1 使用 AI 的理由

表格和文本 PDF 可以用确定性解析。扫描报价和拍照报价包含版面、中文项目名、数量、单位和金额关系，视觉理解能减少逐行录入。AI 只用于抽取候选字段，分类、金额重算、状态变化和权限判断仍由确定性代码执行。

### 7.2 系统流程

```text
上传文件
  ├─ CSV / XLSX → 表头映射和确定性解析
  ├─ 文本 PDF → 文本层和表格提取
  └─ 扫描 PDF / 图片
       ↓
     视觉模型抽取
       ├─ 低质量 → 更强模型复核
       ├─ 视觉失败 → OCR 加二次结构化
       └─ 模型服务异常 → 本地 Tesseract
       ↓
统一 Schema 校验
       ↓
金额重算、来源定位和置信度
       ↓
待校对草稿
       ↓
人工修改和确认
```

### 7.3 输出契约

每个条目至少需要提供原始名称、标准名称、数量、单位、合价、来源位置和字段置信度。单价、空间、类别、材料和工艺允许缺失，但界面必须显式提示。

模型输出必须经过固定 Schema 校验。模型不得直接写入合同基线、批准增减项、修改正式金额、判定价格合理性或生成付款建议。

### 7.4 失败与回退

- 文件损坏、加密、类型伪装或空文件时立即失败。
- 没有识别出完整报价行时不生成空洞草稿。
- 低分辨率或复杂网格无法可靠恢复时提示改用模板或手工录入。
- 模型调用超时、限流或服务异常时执行有限重试，然后回退本地 OCR。
- 回退仍失败时保留原文件、失败原因和人工重试入口。
- 错误信息和日志不得包含模型密钥或完整报价正文。

### 7.5 质量门槛

| 指标 | 公式 | 公开发布门槛 |
|---|---|---|
| 条目召回率 | 成功提取条目数 / 原报价实际条目数 | 不低于 95% |
| 关键金额字段准确率 | 正确金额字段数 / 已核验金额字段数 | 不低于 98% |
| 严重文件错误率 | 影响总额或条目归属的错误文件数 / 样本文件数 | 不高于 1% |
| 人工字段修正率 | 用户修改字段数 / AI 输出字段数 | 不高于 10% |
| 解析成功率 | 进入待校对状态的文件数 / 有效上传文件数 | 不低于 95% |

当前合成样本、扫描扰动样本和少量模型冒烟足以支持内部和小范围 Beta，不足以证明公开发布门槛。公开 Beta 前应建立经过授权的真实报价集，覆盖正常、复杂网格、低清晰度、长文档、异常格式和失败输入。

### 7.6 成本和观测

公开 Beta 前，每个解析任务需要记录模型供应商、模型版本、解析器版本、输入输出用量、延迟、重试次数、回退路径、人工修正率和估算成本。项目级成本上限、异常用量告警和模型变更回归尚未完成。

### 7.7 AI 风险

| 风险 | 预防 | 检测 | 回退 | 发布阻断条件 |
|---|---|---|---|---|
| 金额或归属错误 | Schema、来源定位、金额重算、人工确认 | 金额准确率和严重文件错误率 | 手工模板 | 严重文件错误率超门槛 |
| 模型漂移 | 固定评测集和版本记录 | 同版本分组质量趋势 | 回滚模型或解析器 | 回归未通过 |
| 供应商故障 | 超时和有限重试 | 失败率与延迟告警 | 本地 OCR 或手工录入 | 无可用回退 |
| 成本失控 | 页数、文件大小和任务次数限制 | 单任务与项目成本 | 停用模型解析 | 无成本上限 |
| 文档内容诱导模型 | 固定抽取指令、无业务工具权限 | Schema 异常和越界字段 | 丢弃结果 | 模型可触发业务写入 |
| 敏感资料泄露 | 最小传输、私有存储和供应商条款审查 | 访问日志和安全事件 | 停止外部模型 | 隐私边界不明确 |

---

## 8. 数据、隐私与安全

### 8.1 当前核心数据对象

```text
User
├── LoginChallenge
├── LoginSession
├── NotificationPreference
└── ProjectMembership

Project
├── ProjectBudgetCategory
├── ProjectFundLimitHistory
├── Quote
│   ├── QuoteItem
│   ├── QuoteCorrection
│   └── QuoteParseJob
├── QuoteMatchGroup
├── BaselineVersion
├── ChangeOrder
├── PaymentMilestone
│   ├── AcceptanceRecord
│   └── PaymentRecord
├── Evidence
├── Notification
├── ProjectExportJob
│   └── ProjectExportArtifact
└── AuditEvent
```

### 8.2 数据规则

- 所有业务对象必须能够反查所属项目。
- 金额统一使用整数分，禁止二进制浮点金额入库。
- 服务端系统时间使用 UTC，界面按 Asia/Shanghai 展示。
- 原始文件使用随机对象键，用户文件名只作为元数据。
- 登录、邀请和外部确认令牌只保存摘要。
- 生产环境的项目原件、正式档案和备份存放在私有对象存储；本地开发使用被 Git 忽略的私有目录。
- 下载使用项目权限校验和短时签名地址。
- 关键业务变化追加审计事件。
- 项目到期删除必须清理业务数据、通知、原件和导出制品。当前本地与 S3 兼容存储均已接入，版本化存储同时删除历史版本和删除标记。
- 删除项目的数据可能暂时存在于加密数据库恢复点中，直到备份保留期结束。公开 Beta 前必须定义恢复点的最终清理期限，并在隐私政策中如实说明这一时间窗口。

### 8.3 安全要求

- 线上全站 HTTPS，安全 Cookie 和 CSRF 校验必须启用。
- 生产密钥只通过部署 Secret 注入，不进入仓库、前端或日志。
- 生产环境开启上传时强制配置 ClamAV。关闭上传时，报价和证据上传统一拒绝，就绪检查把扫描器标记为不适用，不能把“关闭扫描”误写成“文件已通过安全检查”。
- 项目访问由服务端成员关系校验，不能依赖前端隐藏按钮。
- 私有对象下载前校验项目权限和对象归属。
- 导出制品下载前校验有效期、大小和 SHA-256。
- 外部确认和家庭邀请链接不可预测、可失效且有期限。
- 敏感操作要求当前会话近期验证；外部决定使用版本条件更新，邀请只能由受邀邮箱的现有会话接受。
- 只有来自显式可信代理 CIDR 的转发头可以参与客户端地址判断，金额字段统一设置服务端上限。
- 备份必须定期生成并通过非破坏性恢复验证。
- 越权、对象引用、文件上传、链接重放、嵌套模态框与恶意文件代码路径已有自动化回归；生产 ClamAV 和目标浏览器仍需部署验收。
- API 和 Worker 启动前校验数据库处于 Alembic 最新版本。后端就绪检查内部综合数据库版本、SQLite 外键、备份、Worker 和 ClamAV 状态；生产响应只返回总体状态，不公开详细运维信息。
- 版本库不得提交运行时上传文件、活动数据库、环境变量或密钥。首次提交前必须检查所有未跟踪文件和嵌套数据目录。

### 8.4 隐私要求

- 地址保持选填。
- 外部确认页与公开邀请页使用固定泛化项目名，不展示用户填写的项目名称、完整地址和项目其他数据。
- 邮件摘要不得携带合同正文、照片或地址。
- 不默认把用户文件用于通用模型训练。
- 使用外部模型前应在隐私政策中说明处理目的、数据范围、服务商和保留方式。
- 用户可以导出和删除项目。
- 客服读取项目数据需要用户授权、明确范围和时限。当前尚无客服授权后台。
- 隐私政策、服务协议和数据处理说明是公开发布前置项。

---

## 9. 非功能需求与部署边界

### 9.1 响应式和可访问性

- 支持近期版本的 Chrome、Safari 和 Edge，以及 iOS Safari、Android Chrome。
- 外部确认流程需要在微信内置浏览器完成兼容验收。
- 桌面和 390px 手机宽度不得出现页面级横向滚动。
- 风险同时使用文字、图标和颜色。
- 表单错误定位到具体字段。
- 所有交互按钮具有可访问名称，主要流程支持键盘操作。
- 用户选择减少动画时降低非必要动效。

### 9.2 性能目标

| 项目 | 目标 | 当前证据 |
|---|---|---|
| 常规页面首屏可用 | 正常 4G 下不超过 3 秒 | 尚无持续监控基线 |
| 常规交互 API P95 | 不超过 800 毫秒 | 尚无线上指标面板 |
| 预算变化刷新 | 不超过 5 秒 | 功能路径通过，尚无计时测试或线上性能证据 |
| 报价和档案任务 | 异步、可恢复、可重试 | 已实现 |
| 正式档案成功率 | 不低于 99% | 需要真实使用样本 |

### 9.3 最近一次已验收线上拓扑

```text
浏览器
  ↓ HTTPS
Next.js 前端
  ↓ 同源代理
FastAPI 后端，固定 1 个实例
  ├── SQLite 活动数据库
  ├── 内嵌报价任务
  ├── 内嵌档案任务
  └── 私有 TOS
       ├── 原始文件
       ├── 导出制品
       └── 不可变 SQLite 恢复点
```

该拓扑在 2026-08-31 完成过 UAT，但不能直接作为加固版的目标拓扑。完整生产上传需要为后端增加 VPC 连接，并通过私网 CLB 访问独立 ClamAV 微服务。目标结构如下。

```text
浏览器 → Next.js 前端 → FastAPI 单实例 → 私有 TOS
                                 └→ VPC → 私网 CLB:3310 → ClamAV 微服务
```

安全组只允许后端访问 3310，不向公网暴露 clamd。完成该拓扑、安全配置、历史文件复扫和主路径复验后，可恢复个人和小范围 Beta。后端仍固定 1 个实例，不支持多实例并发写入、跨地域容灾或无状态扩容。

作品集演示不启用上述 ClamAV 链路。它使用同一套生产认证和业务代码，把 `UPLOADS_ENABLED` 与 `AI_ENABLED` 设为 `false`。报价导入和证据上传入口不可操作，后端继续执行服务端拒绝。已有脱敏样例可以用于查看预算、增减项、验收付款和档案结果。

### 9.4 已准备的扩容形态

PostgreSQL 切换工具、维护模式、独立双队列 Worker、租约、心跳、健康检查和数据库连接池配置已经具备代码基础。当前没有购买或启用生产 PostgreSQL 和独立 Worker。

出现以下任一情况后启动扩容评审。

- 需要两个或更多 API 实例。
- SQLite 锁等待或写失败影响用户。
- 后台任务占用 API 资源并影响交互延迟。
- 单实例恢复时间不满足业务要求。
- 活跃项目和并发任务达到压测阈值。

### 9.5 备份与恢复

- SQLite 使用 Online Backup API 生成一致性副本。
- 每个恢复点记录大小、SHA-256 和 SQLite 完整性结果。
- 先写不可变快照，再更新 latest 副本和清单。
- 恢复前同时校验大小、SHA-256 和 `PRAGMA integrity_check`。
- 默认保留 24 个不可变恢复点。
- 备份异常默认把就绪状态标为 degraded，不直接中断业务。
- 部署环境可以执行非破坏性恢复验证，不覆盖活动数据库。

### 9.6 公开 Beta 运维缺口

- 应用内维护器已周期处理过期制品、延迟删除、暂存文件和孤立档案，应用内备份线程也会生成恢复点；邮件摘要、远端恢复验证和备份告警仍需部署平台调度。
- 调度失败、项目删除失败、死信任务、备份过期、对象存储权限和 SMTP 失败告警。
- API 延迟、错误率、任务耗时、队列深度和模型成本面板。
- 恢复完整生产上传前部署 ClamAV，填写生产配置，并用正常样本、EICAR 测试样本和扫描服务不可用三种路径验收失败关闭策略。当前建议采用同地域独立微服务、私网 CLB 与 VPC 访问，避免公开暴露 clamd 的 3310 端口；新增微服务和 CLB 会产生费用，创建前需要确认预算。
- 切流前执行 `python rescan_source_files.py --include-skipped`，要求全部历史原件扫描为 `clean`，且结果中 `skipped` 和 `error` 均为 0。线上原件数量必须在部署前单独盘点，不以本地样本数量代替。
- 把后端 `/health/ready` 和前端 `/health` 配置为平台探针，对后端就绪路径限制来源或限流，并为持续 503、ClamAV 不可用和病毒库过期建立告警。前端存活不代表后端已就绪。
- 明确数据库恢复点的数据保留与最终清理；项目原件和档案的对象历史版本清理代码已经完成。
- 当前固定单实例模式不以 PostgreSQL 验收作为公开 Beta 前置条件。触发扩容后，必须完成真实 PostgreSQL 的并发领取、租约回收和容量验收。

---

## 10. 指标体系

### 10.1 北极星指标

北极星指标为受控付款率。

当前实现口径如下。

```text
受控付款率 V0
= 非 high_risk 的有效付款金额 / 全部有效付款金额
```

目标产品口径如下。

```text
受控付款率 V1
= 同时满足以下条件的有效付款金额 / 全部有效付款金额

1. 关联合同基线或已批准增减项
2. 关联明确付款节点
3. 节点存在有效验收记录
4. 没有未说明的强制跳过
5. 存在付款凭证或凭证缺失原因
```

当前数据模型只能完整计算 V0。公开 Beta 前补齐业务关联和付款凭证口径后切换到 V1，切换时间必须进入指标版本记录。

### 10.2 主动跟踪指标

| 指标 | 公式 | 当前基线 | 决策用途 |
|---|---|---|---|
| 有效激活率 | 24 小时内完成基线、一个付款节点和一项证据的项目 / 新建项目 | 未建立 | 判断首次设置是否过重 |
| 受控付款率 | 受控付款金额 / 全部有效付款金额 | 未建立 | 判断产品是否改善付款前控制 |
| 事前增项记录率 | 实施或付款前创建的增减项 / 全部增减项 | 当前无法完整计算 | 判断记录是否发生在风险之前 |
| 外部确认完成率 | 有效期内完成处理的外部链接 / 已发送有效链接 | 未建立 | 判断外部流程是否可用 |
| 项目档案成功率 | 成功生成任务 / 全部有效生成请求 | 仅测试样本 | 判断异步任务和文件兼容性 |

旧版 PRD 中的 90 天目标保留为假设，不直接作为当前承诺。先收集至少 20 个真实 Beta 项目或 30 天数据，再确定基线、目标、负责人和低于目标时的动作。

### 10.3 反向指标

| 指标 | 警戒线 | 动作 |
|---|---|---|
| 严重金额计算错误 | 大于 0 | 暂停相关写入并修复 |
| AI 严重文件错误率 | 高于 1% | 降级对应输入类型 |
| 权限或隐私严重事件 | 大于 0 | 启动安全响应并暂停受影响能力 |
| 无效预警率 | 高于 10% | 调整规则并回归 |
| 高风险强制记录金额占比 | 高于 20% | 检查付款条件和录入负担 |
| 外部链接失败率 | 高于 3% | 检查有效期、兼容性和访问性能 |

### 10.4 埋点边界

当前审计事件用于事实追溯，不等同于完整产品分析埋点。后续埋点不得包含合同正文、完整地址、图片内容、银行卡号、验证码、会话令牌或模型密钥。

---

## 11. UAT 与验收状态

### 11.1 旧版 12 个 UAT 状态核对

| 编号 | 场景 | 当前结论 | 尚缺内容 |
|---|---|---|---|
| UAT-01 | 三份报价对比并建立基线 | 部分通过 | 线上为 3 份 CSV，仍需 XLSX、文本 PDF 和扫描 PDF 组合验收 |
| UAT-02 | 基线版本升级 | 部分通过 | 已生成 V1、V2，缺补充合同、条目映射和影响预览 |
| UAT-03 | 外部确认增项 | 核心通过 | 后端、公开页、版本并发与重复提交通过；项目工作台缺发起入口，另缺附件预览和版本快照 |
| UAT-04 | 增项修订重签 | 未完整通过 | 缺关键字段编辑升版和版本快照 |
| UAT-05 | 增项拒绝 | 通过 | 拒绝和重复提交回归已覆盖 |
| UAT-06 | 付款前检查 | 核心通过 | 缺节点相关增项判断和凭证强制口径 |
| UAT-07 | 高风险付款 | 通过 | 继续核对受控付款指标版本 |
| UAT-08 | 付款冲正 | 通过 | 幂等、请求指纹、唯一冲正关系和重复冲正均已回归 |
| UAT-09 | 证据关联与导出 | 核心通过 | 哈希、格式、配额和扫描路径已覆盖；缺多对象关联、预览和删除引用检查 |
| UAT-10 | 权限隔离 | 核心通过 | 邀请防接管、近期登录、CSRF 和可信代理已覆盖；缺外部链接主动撤销专项 |
| UAT-11 | 项目归档和恢复 | 通过 | 无 |
| UAT-12 | 项目删除 | 核心通过 | 业务数据、原件、档案、缓存、历史版本和周期重试已覆盖；缺生产失败告警与恢复点时限政策 |

### 11.2 已完成的生产 UAT

2026-08-31 的隔离验收项目完成以下路径。

- QQ 邮箱验证码、安全 Cookie 和 CSRF 写入。
- 创建项目并生成 12 个预算分类和 5 个付款节点。
- 3 份 CSV 共 6 个条目完成解析和对比。
- 连续激活两个合同基线版本。
- 完成一项内部批准增项和一项外部确认减项。
- 预算净变化、预测结算和风险通知正确联动。
- 完成一条验收、一条付款和一份私有证据。
- 生成 7 页正式报告和单卷 ZIP，并通过 SHA-256 校验。
- UAT 后生成新的不可变数据库恢复点并完成非破坏性验证。

这次 UAT 证明主流程能够在当前单实例线上环境运行，不替代真实用户价值验证、AI 真实报价质量评测和规模化稳定性测试。

2026-09-02 对整改后的当前代码重新执行回归，结果如下。

- 后端 157 项测试通过。
- 真实数据库处于 Alembic 最新版本 `f6c2a9b81d30`，空库升级、当前版本和模型差异检查通过。
- 前端 6 个测试文件、31 项测试通过；TypeScript、ESLint 和生产构建通过。
- 后端就绪探针已增加 ClamAV `PING` 检查和独立快速超时；开启上传且扫描服务不可用时返回 503，生产响应只保留总体状态。前端已增加不缓存的存活路由。
- 分批执行的一次性本地真实浏览器验收覆盖近期登录后原请求只重试一次、邀请页邮箱遮罩与安全跳转、外部减项金额符号与版本冲突，以及显式无附件确认、分类预测、空基线文案、公开页地址脱敏和安全响应头；仓库内可重复执行的前端回归为 Vitest 组件、逻辑与路由测试。
- 从仓库根目录、`backend` 目录和任意临时目录启动时，数据库与数据目录解析结果一致。
- 测试前后上传目录只保留 3 个由数据库引用的本地运行文件，均被 Git 忽略，没有再次产生测试孤儿文件。

仓库已增加前后端 CI 工作流；远端运行结果和分支保护要在公开仓库创建后验证。作品集模式不要求 ClamAV；恢复完整生产上传时必须使用稳定的生产 `AUTH_SECRET`，并以可用 ClamAV 和目标环境告警为前置条件。

### 11.3 新增发布验收

公开 Beta 发布验收的当前状态如下。

- UAT-13 已通过。邮箱频率限制、设备撤销、近期登录和 CSRF 已有自动化与浏览器证据。
- UAT-14 部分通过。私有对象越权、短签过期、制品篡改和路径穿越代码路径通过；目标对象存储和 ClamAV 联合验收待完成。
- UAT-15 已通过代码与既有线上验收。生产重部署后仍需再次验证备份过期、损坏快照、空实例恢复和恢复失败降级。
- UAT-16 已通过。报价与档案任务的自动重试、死信、人工重排和重启恢复已有自动化覆盖。
- UAT-17 待完成。尚未建立真实授权报价集的字段级评测和模型变更回归。
- UAT-18 部分通过。周期维护和延迟删除代码路径通过；生产调度可观测性、失败告警和恢复点最终清理待完成。

---

## 12. 发布计划与门槛

### 12.0 开源作品集发布门槛

- 根目录包含明确的开源许可证、贡献指南、安全报告方式和第三方组件声明。
- README 可以从干净克隆启动，区分本地开发、作品集演示和完整生产三种模式。
- 公开文件不包含真实云端地址、桶名、IAM 名称、项目 ID、用户文件或密钥。
- 在线作品集显式关闭上传与 AI 文件解析，页面和接口表现一致。
- 首次提交前检查暂存清单、忽略规则和常见密钥格式，并在干净克隆中执行前后端测试与构建。

### 12.1 小范围 Beta 重启门槛

当前暂停把加固版开放给新的真实用户。满足以下条件后恢复小范围 Beta。

- 配置独立且稳定的生产 `AUTH_SECRET`，接入可用 ClamAV，并完成生产重部署。部署脚本必须增量写入环境变量，不得整批替换已有 Secret。
- 切流前完成历史原件复扫，要求无 `skipped` 和 `error`。
- 启用后端就绪、前端存活的平台探针，对就绪路径限制来源或限流，并对持续 503、扫描不可用和病毒库过期告警。
- 后端固定单实例。
- 页面持续提示 AI 结果需人工校对。
- 用户知道平台只记录付款事实，不处理装修款。
- 备份健康保持可见，定期执行非破坏性恢复验证。
- 验证上传、外部确认、邀请、近期登录、导出和删除主路径，并接入失败告警。
- 发生阻断问题时可以暂停新用户进入。

### 12.2 公开 Beta 前五项必做

1. 用真实授权报价集完成字段级 AI 评测、成本记录和模型回归。
2. 补齐基线合同关联、项目状态入口、外部确认发起与不可变版本、付款节点编辑、复验和问题明细、付款凭证及超额事实记录。
3. 为邮件摘要、远端恢复验证与备份告警启用部署平台周期调度，补齐维护失败告警，并明确过期恢复点的最终清理政策。
4. 配置并复核生产 `AUTH_SECRET`，部署 ClamAV，完成对象存储和目标浏览器安全验收，并发布隐私政策、服务协议和外部模型数据处理说明。
5. 建立核心指标版本、事件采集和首批真实项目基线。

### 12.3 公开付费前门槛

- 完成真实用户访谈和持续使用复盘。
- 证明至少一项核心价值指标改善，并且反向指标未越线。
- 验证项目制付费意愿、价格和退款预期。
- 完成项目权益、订单、退款、发票和访问控制。
- 原始 12 个 UAT 和新增 6 个 UAT 全部通过。
- 明确客户支持、数据授权和安全事件响应流程。

### 12.4 扩容门槛

需要多实例、任务隔离或更高恢复目标时，执行 SQLite 到 PostgreSQL 切换，并发布独立 Worker。切换必须包含维护窗口、源库快照、空目标校验、逐表行数核对、并发领取、强制终止和租约回收验收。

---

## 13. 延后范围

| 功能 | 当前不做的原因 | 重新评估条件 |
|---|---|---|
| 原生 iOS 和 Android | 响应式 Web 已覆盖核心路径 | 移动 Web 明显限制任务完成率 |
| 微信小程序或服务号 | 增加平台接入和审核成本 | 微信内访问或通知到达成为主要瓶颈 |
| 认证电子签名 | 需要合规服务和身份认证 | 普通确认无法满足真实项目证据需求 |
| 装修款支付和托管 | 资金责任和合规风险高 | 获得合规合作方并有明确用户需求 |
| 地区市场价判断 | 缺少合法、稳定和可更新的数据 | 获得足量地区数据与评测方法 |
| 图片质量自动验收 | 误判责任高 | 获得专业标注和清晰责任边界 |
| 监理多项目工作台 | 当前聚焦业主单项目 | 独立监理渠道形成持续需求 |
| 装修公司 ERP | 与业主独立底账定位不同 | 单独立项 |
| 建材商城和供应链 | 偏离费用控制主流程 | 核心产品稳定后重新评估 |
| 任意多级审批流 | 当前单次确认覆盖主路径 | 大量项目重复出现复杂审批需求 |

---

## 14. 法律与文案边界

禁止使用以下承诺。

- “保证不超支”
- “确认记录具有绝对法律效力”
- “AI 已判定报价合理”
- “平台确认施工合格”
- “平台担保装修款安全”
- “支付成功”

允许使用以下表述。

- “帮助整理和追踪预算变化”
- “保留用户上传和确认记录”
- “提示付款前尚未完成的条件”
- “供双方核对或交由专业人士参考”
- “AI 提取可能有误，请以原始文件和人工确认为准”
- “已记录付款”

正式报告必须保留以下声明。

> 本报告根据用户提交的资料和操作记录自动整理，不构成工程质量鉴定、价格审定、法律意见或付款建议。涉及争议时，请结合原始合同、现场事实并咨询具备资质的专业人士。

---

## 15. 主要风险与负责人

| 风险 | 影响 | 当前控制 | 下一责任方 |
|---|---|---|---|
| 报价识别错误 | 错误预算影响决策 | 来源、置信度、金额重算、人工确认 | 产品与 AI 工程 |
| 增减项版本不完整 | 确认内容发生争议 | 状态和时间线已保留 | 产品与后端 |
| 付款证据关联不足 | 受控付款口径偏弱 | 节点、验收和高风险原因 | 产品与后端 |
| 单实例 SQLite 故障 | 服务中断或数据恢复压力 | 固定单实例、私有恢复点和完整性检查 | 工程与运维 |
| 周期任务缺少生产可观测性 | 邮件、备份或清理失败不能及时发现 | 应用内维护器、可执行脚本和重试状态 | 运维 |
| 恶意附件 | 文件安全风险 | 文件特征、MIME、压缩包与图片边界、SHA-256、私有存储；生产开启上传时强制扫描 | 安全与运维 |
| 生产安全配置未就绪 | 加固版本无法安全启动，或完整模式无法上传附件 | 启动时拒绝弱密钥；生产开启上传时拒绝缺少 ClamAV 的配置 | 运维 |
| 外部确认身份有限 | 用户高估证据强度 | 姓名、角色、时间、IP 摘要和免责声明 | 产品与法务 |
| 用户录入负担 | 使用中断 | 默认模板、AI 草稿和移动端入口 | 产品与设计 |
| 真实需求证据不足 | 公开发布后低使用或低付费 | 重部署后只恢复小范围 Beta，并建立真实使用基线 | 产品 |

---

## 16. 产品决策

### 开源作品集模式 GO

个人面试展示采用关闭上传的生产配置，不购买常驻 ClamAV。仓库保留完整扫描能力、适配镜像、失败关闭测试和部署说明。这个决定只缩小在线演示的输入范围，不删除完整生产能力。

### 生产重部署前 HOLD

当前仓库已经完成一条可运行、可恢复、可导出的核心路径，但不对任何历史线上实例作公开可用承诺。使用独立且稳定的生产 `AUTH_SECRET`，接入 ClamAV 和告警并完成完整模式验收后，才恢复个人及小范围真实文件使用。

### 公开付费发布保持 INVESTIGATE

公开付费前需要证明真实用户持续使用和愿意付费，并补齐基线、增减项版本、验收问题、付款凭证、周期运维、政策和权益系统。没有这些证据时，不把工程 UAT 等同于市场验证。

---

## 17. 版本变更摘要

| V1.0 需求 | V1.1 调整 |
|---|---|
| 产品状态直接标为正式 GO | 加固版生产重部署前 HOLD，验收后再恢复小范围 Beta；公开付费保持 INVESTIGATE |
| 手机或邮箱注册、短信或邮箱登录、密码重置 | 收敛为邮箱无密码登录，短信延后，密码重置不适用 |
| 单项目核心流程 | 增加多项目中心、三步建项和完整生命周期 |
| 家庭协作者为规划能力 | 已完成邀请、角色、权限隔离和安全会话 |
| 报价解析为功能要求 | 增加异步任务、回退流程、人工匹配、死信和心跳 |
| 风险提示为页面能力 | 增加 A1 至 A8 持久通知、去重、解除和邮件摘要执行器 |
| 项目导出为单个文件 | 升级为正式多页报告、私有对象、SHA-256 和独立分卷 |
| 生产部署未定 | 当前采用单实例 veFaaS、SQLite、私有 TOS 和不可变恢复点 |
| PostgreSQL 和独立 Worker 为后续方向 | 代码与切换工具已准备，当前没有启用付费资源 |
| 项目购买权益属于 V1 | 调整为待验证和待实现，不再描述为当前能力 |
| 90 天指标直接给定目标 | 先建立真实 Beta 基线，旧目标降为假设 |
| 2026-09-02 安全与完整性整改 | 增加上传扫描与配额、金融幂等、删除完整流程、近期登录、数据库版本门禁、前端存活路由、ClamAV 就绪门禁、前端回归和持续集成 |
| 2026-09-02 开源作品集决策 | 增加生产上传总开关、零新增扫描费用的演示模式、开源治理和资源信息脱敏；完整生产上传继续强制 ClamAV |

---

## 18. 关联材料

- [旧版正式 PRD V1.0](./renovation-budget-change-manager-prd-v1.md)
- [项目 README](./README.md)
- [阶段 1 技术开发文档](./docs/阶段1技术开发文档-Alpha核心闭环.md)
- [阶段 2 技术开发文档](./docs/阶段2技术开发文档-Beta报价智能化.md)
- [阶段 3 技术开发文档](./docs/阶段3技术开发文档-Beta异步任务与人工匹配.md)
- [阶段 4 技术开发文档](./docs/阶段4技术开发文档-Beta家庭协作与权限.md)
- [阶段 5 技术开发文档](./docs/阶段5技术开发文档-生产认证与安全会话.md)
- [阶段 6 技术开发文档](./docs/阶段6技术开发文档-项目启用与生命周期.md)
- [阶段 7 技术开发文档](./docs/阶段7技术开发文档-风险预警与通知中心.md)
- [阶段 8 技术开发文档](./docs/阶段8技术开发文档-异步档案与事件通知.md)
- [阶段 9 技术开发文档](./docs/阶段9技术开发文档-正式报告与私有制品.md)
- [阶段 10 技术开发文档](./docs/阶段10技术开发文档-生产队列对象存储与分卷.md)
- [阶段 11 技术开发文档](./docs/阶段11技术开发文档-报价队列心跳与PostgreSQL准备.md)
- [阶段 12 技术开发文档](./docs/阶段12技术开发文档-火山引擎临时HTTPS部署.md)
- [阶段 13 技术开发文档](./docs/阶段13技术开发文档-TOS持久化与云备份验收.md)
- [阶段 14 技术开发文档](./docs/阶段14技术开发文档-PostgreSQL切换与独立Worker服务.md)
- [阶段 15 技术开发文档](./docs/阶段15技术开发文档-低成本单实例上线加固.md)
- [阶段 16 技术开发文档](./docs/阶段16技术开发文档-真实邮箱与生产UAT.md)
- [阶段 17 技术开发文档](./docs/阶段17技术开发文档-生产健康探针与ClamAV接入准备.md)
- [正式项目档案样例](./output/pdf/阶段9-项目档案报告样例.pdf)
